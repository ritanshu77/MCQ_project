{
  "name": "mcq-project-monorepo",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install concurrently && cd exam-frontend && npm install && cd ../exam-backend-api && npm install",
    "postinstall": "cd exam-frontend && npm install && cd ../exam-backend-api && npm install",
    "dev": "npx concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd exam-frontend && npm run dev",
    "dev:backend": "cd exam-backend-api && npm run start:dev",
    "build": "cd exam-frontend && npm run build && cd ../exam-backend-api && npm run build",
    "start": "npx concurrently \"npm run start:frontend\" \"npm run start:backend\"",
    "start:frontend": "cd exam-frontend && npm start",
    "start:backend": "cd exam-backend-api && npm run start:prod"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.18"
  }
}
